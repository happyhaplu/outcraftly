# This is an example of your .env file format, which pnpm db:setup will create.
# Note: this must be .env, not .env.local, without further configuration changes.

# --- Required secrets & infrastructure ---
# Required: PostgreSQL connection string. The default matches docker-compose.yml.
POSTGRES_URL=postgresql://postgres:postgres@localhost:54322/postgres
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
POSTGRES_DB=postgres
SMTP_PASSWORD=change-me
IMAP_PASSWORD=change-me
# Required: Stripe secret key (test or live, depending on environment).
STRIPE_SECRET_KEY=sk_test_yourSecretKey
# Required: Stripe webhook signing secret from `stripe listen --print-secret` or dashboard.
STRIPE_WEBHOOK_SECRET=whsec_yourWebhookSecret
# Required: Public base URL of the app (used for redirects and worker callbacks).
BASE_URL=http://localhost:3000
# Required: Auth signing secret (32+ bytes). Generate with `openssl rand -base64 32`.
AUTH_SECRET=replace-with-strong-secret
# Required: 32+ character encryption key for storing sender credentials.
SENDER_CREDENTIALS_KEY=your-32-character-secret-must-be-long
# Required: HMAC secret to validate inbound sequence events webhook.
SEQUENCE_EVENTS_SECRET=replace-with-random-sequence-events-secret
# Required: Secret used by scheduled sequence worker invocations.
SEQUENCE_WORKER_SECRET=replace-with-sequence-worker-token
# Required: Secret used by scheduled reply detection worker invocations.
REPLY_WORKER_SECRET=replace-with-reply-worker-token

# --- Tunables & optional diagnostics ---
# Optional: Minimum minutes between sequence sends (defaults to 5 when unset).
MIN_SEND_INTERVAL_MINUTES=5
# Optional: Enable verbose inbound mail logging when set to "true".
DEBUG_INBOUND=false